# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    env:
      VITE_GITHUB_API_TOKEN: ${{ secrets.VITE_GITHUB_API_TOKEN }}
      VITE_GITLAB_API_TOKEN: ${{ secrets.VITE_GITLAB_API_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - run: npm i pnpm -g && pnpm i && pnpm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_GIT_BOARD_B6A6F }}
          channelId: live
          projectId: git-board-b6a6f
